language: java
jdk: openjdk11
install:
 - mvn install -DskipTests=true  -B
script:
 - mvn clean package -DskipTests=true
before_install:
 - cd regedit-backend-service
 - chmod +x mvnw
before_script:
 - cd regedit-backend-service
 - chmod +x mvnw
